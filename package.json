{
  "name": "smartdoc",
  "version": "1.0.0",
  "private": true,
  "description": "SmartDoc - CSV to Template-Based PDF Generator",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.1",
    "papaparse": "^5.4.1"
  }
}
